const KEY="xskt-lab-forecasts-v1";export function list(){try{return JSON.parse(localStorage.getItem(KEY)||"[]")}catch{return[]}}export function save(record){const rows=list();const unique=rows.some(x=>x.province===record.province&&x.targetDate===record.targetDate&&x.version===record.version);if(unique)throw new Error("Đã lưu dự báo này");rows.unshift(Object.freeze({...record,id:crypto.randomUUID(),savedAt:new Date().toISOString()}));localStorage.setItem(KEY,JSON.stringify(rows));return rows}export function reconcile(rows,draws){return rows.map(x=>{const actual=draws.find(d=>d.province===x.province&&d.date===x.targetDate)?.tail??null;return{...x,actual,rank:actual?x.top.indexOf(actual)+1:null}})}