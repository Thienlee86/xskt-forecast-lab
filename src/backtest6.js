import{predict6}from"./model6.js";
const samePositions=(a,b)=>[...a].reduce((n,d,i)=>n+(d===b[i]),0);
export async function backtest6(draws,{testSize=30,window=100,onProgress}={}){const start=Math.max(20,draws.length-testSize);let n=0,exact10=0,exact50=0,pos=0,pair0=0,pair1=0,pair2=0,threePlus=0;for(let i=start;i<draws.length;i++){const result=predict6(draws.slice(0,i),{window,top:50}),actual=draws[i].db,list=result.ranking.map(x=>x.number),m=samePositions(actual,list[0]);n++;exact10+=list.slice(0,10).includes(actual);exact50+=list.includes(actual);pos+=m/6;threePlus+=m>=3;pair0+=result.ranking.slice(0,10).some(x=>x.pairs[0]===actual.slice(0,2));pair1+=result.ranking.slice(0,10).some(x=>x.pairs[1]===actual.slice(2,4));pair2+=result.ranking.slice(0,10).some(x=>x.pairs[2]===actual.slice(4,6));if(i%5===0){onProgress?.(i-start,draws.length-start);await new Promise(r=>setTimeout(r,0))}}return{tested:n,exact10:exact10/n,exact50:exact50/n,positionAccuracy:pos/n,threePlus:threePlus/n,pairAB:pair0/n,pairCD:pair1/n,pairEF:pair2/n}}
